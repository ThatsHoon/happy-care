# 블루&화이트 디자인 시스템 + 키오스크 리스킨 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 블루&화이트 디자인 토큰과 공통 컴포넌트(RadialGauge·IconChip·StatCard)를 만들고, 기존 키오스크를 원형 상태 링 + 블루/화이트로 리스킨한다(기존 6개 테스트 유지).

**Architecture:** `theme.css`에 토큰(단일 진실원) → 자체 스타일 SVG `RadialGauge` + 카드 프리미티브 → `Kiosk.tsx`가 상태 히어로를 pulse-ring 대신 `RadialGauge`로 교체하고 블루/화이트 클래스로 재도색. 스토어·WS·테스트 앵커는 불변.

**Tech Stack:** React 18 · Vite · TypeScript(strict) · lucide-react · framer-motion · Vitest · @testing-library/react

## Global Constraints

- 레포 루트: `C:\Users\erifkim\my_projects\K-hackathon\happy-care`. 웹은 `web/`. Windows(Git Bash), 테스트 `cd web && npm run test`, 타입 `npx tsc -b`.
- **이모지 절대 금지** — 아이콘은 lucide-react만.
- 팔레트 토큰(정확값): `--ground:#F4F7FE --card:#FFFFFF --ink:#0F1B33 --muted:#5B6B87 --faint:#8A97B2 --line:#E4EAF5 --blue:#2E6BF6 --blue-deep:#1B4DB3 --blue-soft:#EAF1FF --good:#1FA97A --good-soft:#E3F5EE --danger:#E5484D --danger-soft:#FCE9E9 --amber:#E8A23D`. 히어로 그라데이션 `#1E5BE6 → #5B9BFF`.
- 폰트 스택: `'Pretendard','Apple SD Gothic Neo','Malgun Gothic',system-ui,-apple-system,sans-serif`.
- **테스트 앵커 유지**: `data-testid="mode"`가 실내→"실내 모드"·실외→"실외 모드" 텍스트를 포함, 낙상 오버레이는 `role="alert"`에 "낙상 감지" 포함. 기존 6개 테스트(store 3·Kiosk 2·App 1) 계속 통과.
- TypeScript **strict**. 접근성: 키오스크 본문 텍스트는 `--ink`(딥 네이비), 블루는 큰 구조/액센트로만.

---

## File Structure

```
web/src/
  theme.css                 # 신규: 토큰 + base + 공통 컴포넌트 클래스(icon-chip/stat-card)
  main.tsx                  # 수정: theme.css import 추가
  index.css                 # 수정: 키오스크 클래스를 블루/화이트로 재작성
  components/
    RadialGauge.tsx         # 신규: SVG 원형 게이지
    IconChip.tsx            # 신규: 아이콘 배경 칩
    StatCard.tsx            # 신규: 지표 카드
    Skeleton.tsx  ActionTile.tsx   # 기존 유지(재도색은 index.css에서)
  views/Kiosk.tsx           # 수정: 상태 히어로를 RadialGauge로 교체 + 블루/화이트
  tests/
    RadialGauge.test.tsx  IconChip.test.tsx  StatCard.test.tsx   # 신규
```

---

### Task 1: 디자인 토큰(theme.css) + RadialGauge

**Files:**
- Create: `web/src/theme.css`
- Modify: `web/src/main.tsx` (import 추가)
- Create: `web/src/components/RadialGauge.tsx`
- Test: `web/tests/RadialGauge.test.tsx`

**Interfaces:**
- Produces: `RadialGauge(props: { value:number; size?:number; stroke?:number; from?:string; to?:string; track?:string; children?:ReactNode })` — SVG 원형 게이지. 진행 원의 `stroke-dasharray = 2πr`, `stroke-dashoffset = 2πr·(1-clamp(value,0,1))`, `data-role="progress"`. `r=(size-stroke)/2`. 가운데 children 슬롯.

- [ ] **Step 1: RadialGauge 테스트 작성(실패)**

`web/tests/RadialGauge.test.tsx`:
```tsx
import { render } from '@testing-library/react'
import { expect, test } from 'vitest'
import { RadialGauge } from '../src/components/RadialGauge'

test('progress arc reflects value', () => {
  const { container } = render(<RadialGauge value={0.75} size={200} stroke={16} />)
  const prog = container.querySelector('[data-role="progress"]')!
  const r = (200 - 16) / 2
  const c = 2 * Math.PI * r
  expect(Number(prog.getAttribute('stroke-dasharray'))).toBeCloseTo(c, 2)
  expect(Number(prog.getAttribute('stroke-dashoffset'))).toBeCloseTo(c * (1 - 0.75), 2)
})

test('value is clamped to [0,1]', () => {
  const { container } = render(<RadialGauge value={1.8} size={100} stroke={10} />)
  const prog = container.querySelector('[data-role="progress"]')!
  expect(Number(prog.getAttribute('stroke-dashoffset'))).toBeCloseTo(0, 5)
})

test('renders center children', () => {
  const { getByText } = render(<RadialGauge value={0.5}><span>안심</span></RadialGauge>)
  expect(getByText('안심')).toBeInTheDocument()
})
```

- [ ] **Step 2: 테스트 실패 확인**

Run: `cd web && npm run test`
Expected: FAIL — cannot find `../src/components/RadialGauge`

- [ ] **Step 3: theme.css 생성 + main.tsx import + RadialGauge 구현**

`web/src/theme.css`:
```css
:root{
  --ground:#F4F7FE; --card:#FFFFFF;
  --ink:#0F1B33; --muted:#5B6B87; --faint:#8A97B2; --line:#E4EAF5;
  --blue:#2E6BF6; --blue-deep:#1B4DB3; --blue-soft:#EAF1FF;
  --good:#1FA97A; --good-soft:#E3F5EE;
  --danger:#E5484D; --danger-soft:#FCE9E9; --amber:#E8A23D;
  --radius:20px; --radius-lg:26px;
  --shadow-sm:0 8px 22px -12px rgba(24,54,120,.28);
  --font:'Pretendard','Apple SD Gothic Neo','Malgun Gothic',system-ui,-apple-system,sans-serif;
}
*{box-sizing:border-box}
html,body,#root{height:100%;margin:0}
body{background:var(--ground);color:var(--ink);font-family:var(--font);
  -webkit-font-smoothing:antialiased;line-height:1.4}
:focus-visible{outline:3px solid var(--blue);outline-offset:3px;border-radius:12px}
```

`web/src/main.tsx` — `import './index.css'` 위에 `import './theme.css'` 한 줄 추가:
```tsx
import { StrictMode } from 'react'
import { createRoot } from 'react-dom/client'
import './theme.css'
import './index.css'
import App from './App.tsx'

createRoot(document.getElementById('root')!).render(
  <StrictMode>
    <App />
  </StrictMode>,
)
```

`web/src/components/RadialGauge.tsx`:
```tsx
import { useId } from 'react'
import type { ReactNode } from 'react'

interface RadialGaugeProps {
  value: number
  size?: number
  stroke?: number
  from?: string
  to?: string
  track?: string
  children?: ReactNode
}

export function RadialGauge({
  value, size = 200, stroke = 16,
  from = '#1E5BE6', to = '#5B9BFF', track = '#EAF1FF', children,
}: RadialGaugeProps) {
  const id = useId()
  const r = (size - stroke) / 2
  const c = 2 * Math.PI * r
  const v = Math.min(1, Math.max(0, value))
  return (
    <div style={{ position: 'relative', width: size, height: size }}>
      <svg width={size} height={size} style={{ transform: 'rotate(-90deg)' }}>
        <defs>
          <linearGradient id={id} x1="0" y1="0" x2="1" y2="1">
            <stop offset="0" stopColor={from} />
            <stop offset="1" stopColor={to} />
          </linearGradient>
        </defs>
        <circle cx={size / 2} cy={size / 2} r={r} fill="none" stroke={track} strokeWidth={stroke} />
        <circle
          data-role="progress"
          cx={size / 2} cy={size / 2} r={r} fill="none"
          stroke={`url(#${id})`} strokeWidth={stroke} strokeLinecap="round"
          strokeDasharray={c} strokeDashoffset={c * (1 - v)}
        />
      </svg>
      <div style={{
        position: 'absolute', inset: 0, display: 'flex',
        flexDirection: 'column', alignItems: 'center', justifyContent: 'center',
      }}>
        {children}
      </div>
    </div>
  )
}
```

- [ ] **Step 4: 테스트 통과 확인**

Run: `npm run test`
Expected: PASS (RadialGauge 3 + 기존 6 = 9 passed)

- [ ] **Step 5: 타입 확인 + 커밋**

Run: `npx tsc -b` (expect clean)
```bash
cd C:/Users/erifkim/my_projects/K-hackathon/happy-care
git add web/src/theme.css web/src/main.tsx web/src/components/RadialGauge.tsx web/tests/RadialGauge.test.tsx
git commit -m "feat(web): blue/white design tokens + RadialGauge component"
```

---

### Task 2: IconChip + StatCard

**Files:**
- Create: `web/src/components/IconChip.tsx`
- Create: `web/src/components/StatCard.tsx`
- Modify: `web/src/theme.css` (icon-chip/stat-card 클래스 추가)
- Test: `web/tests/IconChip.test.tsx`, `web/tests/StatCard.test.tsx`

**Interfaces:**
- Consumes: 토큰(theme.css, Task 1), lucide-react `LucideIcon`.
- Produces: `IconChip(props:{ icon:LucideIcon; tone?:'blue'|'good'|'danger'; box?:number; size?:number })` — `.icon-chip`(+`is-good`/`is-danger`) span에 아이콘, 크기는 `box`(px, 기본 44), 아이콘 `size`(px, 기본 24). `StatCard(props:{ icon:LucideIcon; label:string; children?:ReactNode })` — `.stat-card` > `.sc-top`(IconChip box=26 size=15 + label) + `.sc-val`(children).

- [ ] **Step 1: 테스트 작성(실패)**

`web/tests/IconChip.test.tsx`:
```tsx
import { render } from '@testing-library/react'
import { HeartPulse } from 'lucide-react'
import { expect, test } from 'vitest'
import { IconChip } from '../src/components/IconChip'

test('renders an svg icon inside a toned chip', () => {
  const { container } = render(<IconChip icon={HeartPulse} tone="danger" />)
  const chip = container.querySelector('.icon-chip')!
  expect(chip.className).toContain('is-danger')
  expect(chip.querySelector('svg')).toBeTruthy()
})
```

`web/tests/StatCard.test.tsx`:
```tsx
import { render, screen } from '@testing-library/react'
import { HeartPulse } from 'lucide-react'
import { expect, test } from 'vitest'
import { StatCard } from '../src/components/StatCard'

test('shows label and value', () => {
  render(<StatCard icon={HeartPulse} label="심박">72</StatCard>)
  expect(screen.getByText('심박')).toBeInTheDocument()
  expect(screen.getByText('72')).toBeInTheDocument()
})
```

- [ ] **Step 2: 테스트 실패 확인**

Run: `cd web && npm run test`
Expected: FAIL — cannot find `../src/components/IconChip` / `StatCard`

- [ ] **Step 3: theme.css 클래스 추가 + 컴포넌트 구현**

`web/src/theme.css` 끝에 추가:
```css
.icon-chip{border-radius:12px;display:grid;place-items:center;flex:none;
  background:var(--blue-soft);color:var(--blue)}
.icon-chip.is-good{background:var(--good-soft);color:var(--good)}
.icon-chip.is-danger{background:var(--danger-soft);color:var(--danger)}

.stat-card{background:var(--card);border-radius:20px;padding:16px;
  box-shadow:var(--shadow-sm);display:flex;flex-direction:column;gap:10px}
.stat-card .sc-top{display:flex;align-items:center;gap:9px;
  color:var(--muted);font-size:13px;font-weight:700}
.stat-card .sc-val{font-size:28px;font-weight:800;letter-spacing:-.02em;
  display:flex;align-items:baseline;gap:5px}
.stat-card .sc-val .u{font-size:14px;font-weight:600;color:var(--faint)}
```

`web/src/components/IconChip.tsx`:
```tsx
import type { LucideIcon } from 'lucide-react'

interface IconChipProps {
  icon: LucideIcon
  tone?: 'blue' | 'good' | 'danger'
  box?: number
  size?: number
}

export function IconChip({ icon: Icon, tone = 'blue', box = 44, size = 24 }: IconChipProps) {
  const cls =
    tone === 'good' ? 'icon-chip is-good' : tone === 'danger' ? 'icon-chip is-danger' : 'icon-chip'
  return (
    <span className={cls} style={{ width: box, height: box }}>
      <Icon size={size} strokeWidth={2.2} aria-hidden="true" />
    </span>
  )
}
```

`web/src/components/StatCard.tsx`:
```tsx
import type { LucideIcon } from 'lucide-react'
import type { ReactNode } from 'react'
import { IconChip } from './IconChip'

interface StatCardProps {
  icon: LucideIcon
  label: string
  children?: ReactNode
}

export function StatCard({ icon, label, children }: StatCardProps) {
  return (
    <div className="stat-card">
      <div className="sc-top"><IconChip icon={icon} box={26} size={15} />{label}</div>
      {children != null && <div className="sc-val">{children}</div>}
    </div>
  )
}
```

- [ ] **Step 4: 테스트 통과 확인**

Run: `npm run test`
Expected: PASS (IconChip 1 + StatCard 1 + 기존 9 = 11 passed)

- [ ] **Step 5: 타입 확인 + 커밋**

Run: `npx tsc -b` (expect clean)
```bash
cd C:/Users/erifkim/my_projects/K-hackathon/happy-care
git add web/src/theme.css web/src/components/IconChip.tsx web/src/components/StatCard.tsx web/tests/IconChip.test.tsx web/tests/StatCard.test.tsx
git commit -m "feat(web): IconChip + StatCard design primitives"
```

---

### Task 3: 키오스크 리스킨 (블루/화이트 + RadialGauge 상태 링)

**Files:**
- Modify: `web/src/views/Kiosk.tsx` (상태 히어로 교체)
- Modify: `web/src/index.css` (전체를 블루/화이트로 재작성)
- Test: 기존 `web/tests/Kiosk.test.tsx` 그대로 통과 (수정 없음)

**Interfaces:**
- Consumes: `RadialGauge`(Task 1), `ActionTile`/`Skeleton`(기존), `useStore`(기존), lucide-react.
- Produces: 없음(최종 화면). `data-testid="mode"`·`role="alert"` 앵커 유지.

- [ ] **Step 1: index.css 를 블루/화이트로 재작성**

`web/src/index.css` 전체 교체:
```css
/* 행복이 키오스크 — 블루 & 화이트 (토큰은 theme.css) */
.kiosk{min-height:100%;max-width:820px;margin:0 auto;padding:26px 26px 32px;
  display:flex;flex-direction:column;gap:26px}

.topbar{display:flex;align-items:center;justify-content:space-between}
.clock{font-size:18px;font-weight:700;color:var(--muted);font-variant-numeric:tabular-nums}
.conn{display:inline-flex;align-items:center;gap:7px;font-size:14px;font-weight:700;
  color:var(--muted);background:var(--card);border:1px solid var(--line);border-radius:999px;padding:7px 14px}
.conn.is-on{color:var(--good);background:var(--good-soft);border-color:transparent}

.kstatus{display:flex;flex-direction:column;align-items:center;text-align:center;gap:4px;padding:4px 0 6px}
.kgauge-icon{display:grid;color:var(--blue)}
.ktitle{font-size:clamp(30px,5vw,40px);font-weight:800;letter-spacing:-.02em;margin:16px 0 0}
.ksub{font-size:20px;font-weight:600;color:var(--muted);margin:6px 0 0}

.grid{display:grid;grid-template-columns:1fr 1fr;gap:16px}
.tile{appearance:none;cursor:pointer;text-align:left;font-family:inherit;
  background:var(--card);border:1px solid var(--line);border-radius:22px;padding:22px;
  min-height:126px;display:flex;flex-direction:column;gap:12px;color:var(--ink);
  box-shadow:var(--shadow-sm);transition:border-color .15s ease,transform .12s ease}
.tile:hover{border-color:color-mix(in srgb,var(--blue) 45%,var(--line))}
.tile:active{transform:scale(.985)}
.tile-icon{width:56px;height:56px;border-radius:16px;display:grid;place-items:center;
  background:var(--blue-soft);color:var(--blue)}
.tile-label{font-size:23px;font-weight:800}
.tile-value{font-size:34px;font-weight:800;letter-spacing:-.02em;
  display:inline-flex;align-items:baseline;gap:6px;font-variant-numeric:tabular-nums}
.tile-value .unit{font-size:17px;font-weight:600;color:var(--faint)}
.tile--danger{background:var(--danger);border-color:var(--danger);color:#fff}
.tile--danger .tile-icon{background:rgba(255,255,255,.2);color:#fff}

.skeleton{display:inline-block;background:linear-gradient(100deg,
  var(--blue-soft) 30%,color-mix(in srgb,var(--blue-soft) 50%,#fff) 50%,var(--blue-soft) 70%);
  background-size:200% 100%;animation:shimmer 1.4s ease-in-out infinite}
@keyframes shimmer{from{background-position:200% 0}to{background-position:-200% 0}}

.fall-scrim{position:fixed;inset:0;z-index:50;display:grid;place-items:center;padding:24px;
  background:color-mix(in srgb,var(--ink) 55%,transparent);backdrop-filter:blur(2px)}
.fall-card{width:min(560px,100%);background:var(--danger);color:#fff;border-radius:28px;
  padding:40px 32px 36px;text-align:center;display:flex;flex-direction:column;align-items:center;gap:14px;
  box-shadow:0 24px 60px color-mix(in srgb,var(--danger) 45%,transparent)}
.fall-icon{width:96px;height:96px;border-radius:50%;display:grid;place-items:center;background:rgba(255,255,255,.16)}
.fall-title{margin:0;font-size:44px;font-weight:800;letter-spacing:-.02em}
.fall-sub{margin:0;font-size:24px;font-weight:600;opacity:.92}

@media (max-width:480px){.grid{grid-template-columns:1fr}.tile{min-height:104px}}
@media (prefers-reduced-motion:reduce){.skeleton{animation:none}}
```

- [ ] **Step 2: Kiosk.tsx 상태 히어로를 RadialGauge로 교체**

`web/src/views/Kiosk.tsx` 전체 교체:
```tsx
import { useEffect, useState } from 'react'
import { AnimatePresence, motion } from 'framer-motion'
import {
  Footprints, HeartPulse, Phone, PersonStanding, ShieldCheck, Siren,
  TriangleAlert, Wifi, WifiOff,
} from 'lucide-react'
import { useStore } from '../store'
import { Skeleton } from '../components/Skeleton'
import { ActionTile } from '../components/ActionTile'
import { RadialGauge } from '../components/RadialGauge'

const WEEKDAYS = ['일', '월', '화', '수', '목', '금', '토']

function formatClock(d: Date): string {
  const h = d.getHours()
  const m = d.getMinutes().toString().padStart(2, '0')
  const ampm = h < 12 ? '오전' : '오후'
  const h12 = ((h + 11) % 12) + 1
  return `${ampm} ${h12}:${m} · ${d.getMonth() + 1}월 ${d.getDate()}일 ${WEEKDAYS[d.getDay()]}`
}

function useClock(): Date {
  const [now, setNow] = useState(() => new Date())
  useEffect(() => {
    const id = setInterval(() => setNow(new Date()), 15000)
    return () => clearInterval(id)
  }, [])
  return now
}

export function Kiosk() {
  const mode = useStore((s) => s.mode)
  const alert = useStore((s) => s.alert)
  const heartBpm = useStore((s) => s.heartBpm)
  const connected = useStore((s) => s.connected)
  const clearAlert = useStore((s) => s.clearAlert)
  const now = useClock()

  const alertTs = alert?.type === 'fall' ? alert.ts : null
  useEffect(() => {
    if (alertTs == null) return
    const id = setTimeout(() => clearAlert(), 6000)
    return () => clearTimeout(id)
  }, [alertTs, clearAlert])

  const isIndoor = mode === 'indoor'
  const StatusIcon = isIndoor ? ShieldCheck : Footprints
  const title = isIndoor ? '지켜보고 있어요' : '함께 걷고 있어요'
  const sub = isIndoor ? '실내 모드 · 카메라 꺼짐' : '실외 모드 · 보행 보조'

  return (
    <div className="kiosk">
      <div className="topbar">
        <span className="clock">{formatClock(now)}</span>
        <span className={connected ? 'conn is-on' : 'conn'}>
          {connected ? <Wifi size={18} aria-hidden="true" /> : <WifiOff size={18} aria-hidden="true" />}
          {connected ? '연결됨' : '연결 중'}
        </span>
      </div>

      <header className="kstatus">
        <RadialGauge value={0.86} size={216} stroke={14}>
          <AnimatePresence mode="wait" initial={false}>
            <motion.span
              key={mode}
              className="kgauge-icon"
              initial={{ opacity: 0, scale: 0.7 }}
              animate={{ opacity: 1, scale: 1 }}
              exit={{ opacity: 0, scale: 0.7 }}
              transition={{ duration: 0.35 }}
            >
              <StatusIcon size={64} strokeWidth={1.9} aria-hidden="true" />
            </motion.span>
          </AnimatePresence>
        </RadialGauge>
        <AnimatePresence mode="wait" initial={false}>
          <motion.div
            key={mode}
            initial={{ opacity: 0, y: 10 }}
            animate={{ opacity: 1, y: 0 }}
            exit={{ opacity: 0, y: -10 }}
            transition={{ duration: 0.3 }}
          >
            <h1 className="ktitle">{title}</h1>
            <p data-testid="mode" className="ksub">{sub}</p>
          </motion.div>
        </AnimatePresence>
      </header>

      <div className="grid">
        <ActionTile icon={HeartPulse} label="건강 상태">
          {heartBpm != null ? (
            <motion.span initial={{ opacity: 0 }} animate={{ opacity: 1 }} transition={{ duration: 0.4 }}>
              {heartBpm}
              <span className="unit">회/분</span>
            </motion.span>
          ) : (
            <Skeleton width={120} height={40} radius={12} />
          )}
        </ActionTile>
        <ActionTile icon={PersonStanding} label="보행 도움" />
        <ActionTile icon={Phone} label="가족 연결" />
        <ActionTile icon={Siren} label="도움 요청" tone="danger" />
      </div>

      <AnimatePresence>
        {alert?.type === 'fall' && (
          <motion.div className="fall-scrim" initial={{ opacity: 0 }} animate={{ opacity: 1 }} exit={{ opacity: 0 }}>
            <motion.div
              role="alert"
              className="fall-card"
              initial={{ opacity: 0, y: 24, scale: 0.96 }}
              animate={{ opacity: 1, y: 0, scale: 1 }}
              exit={{ opacity: 0, scale: 0.96 }}
              transition={{ type: 'spring', stiffness: 300, damping: 26 }}
            >
              <span className="fall-icon">
                <TriangleAlert size={56} strokeWidth={2.2} aria-hidden="true" />
              </span>
              <h2 className="fall-title">낙상 감지</h2>
              <p className="fall-sub">도움을 부르고 있어요</p>
            </motion.div>
          </motion.div>
        )}
      </AnimatePresence>
    </div>
  )
}
```

- [ ] **Step 3: 기존 테스트·타입 확인**

Run: `cd web && npm run test`
Expected: PASS (전체 11 passed — Kiosk 2 기존 테스트 포함)
Run: `npx tsc -b`
Expected: clean

- [ ] **Step 4: 시각 스모크 (선택)**

터미널 A: `cd backend && . .venv/Scripts/activate && uvicorn happy_care.app:app`
터미널 B: `cd web && npm run dev` → 상태가 **블루 원형 링 + 방패 아이콘**, 타일이 블루/화이트, 낙상 시 빨강 오버레이면 성공.

- [ ] **Step 5: 커밋**

```bash
cd C:/Users/erifkim/my_projects/K-hackathon/happy-care
git add web/src/views/Kiosk.tsx web/src/index.css
git commit -m "feat(web): reskin kiosk to blue/white with RadialGauge status ring"
```

---

## Self-Review

**1. Spec coverage (계획 1 범위):**
- 토큰 단일 진실원(theme.css) → Task 1 ✅
- RadialGauge 공통 컴포넌트 → Task 1 ✅
- IconChip·StatCard 프리미티브 → Task 2 ✅
- 키오스크 블루/화이트 리스킨 + 원형 상태 링 → Task 3 ✅
- 테스트 앵커·낙상 오버레이·자동해제 유지 → Task 3(코드에 `data-testid="mode"`, `role="alert"` "낙상 감지", `clearAlert` 6s 유지) ✅
- 범위 밖(계획 2): 보호자 대시보드, StatCard/Sparkbars/지도의 대시보드 사용 — 다음 계획.

**2. Placeholder scan:** TODO/TBD/"적절히" 없음. 모든 스텝에 실제 코드·명령·기대출력 포함 ✅

**3. Type consistency:** `RadialGauge{value,size,stroke,from,to,track,children}` · `IconChip{icon,tone,box,size}` · `StatCard{icon,label,children}` — Task 1·2 정의와 Task 3 사용 일치. Kiosk는 `RadialGauge`만 사용(StatCard는 계획 2 대시보드용). 기존 `ActionTile{icon,label,tone,children}`·`Skeleton{width,height,radius}` 시그니처 불변 ✅

---

## 다음 계획
`구현계획_03_보호자대시보드.md` — `/dashboard` 라우팅(react-router-dom) + 안심 게이지 히어로 + 심박·활동 StatCard + 집 평면도 + 최근 소식 + 하단 탭 + 파생 로직(순수함수·단위테스트).
