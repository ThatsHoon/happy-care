# 보호자 대시보드(/dashboard) Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** `/dashboard` 라우트에 보호자용 모바일 홈 화면(종합 안심 게이지 히어로 · 심박·활동 카드 · 집 평면도 · 최근 소식 · 하단 탭)을 만들고, `react-router-dom`으로 `/kiosk`·`/dashboard`를 라우팅한다.

**Architecture:** 안심 상태·시간 표기는 `lib/derive.ts`의 순수 함수로 분리(store/컴포넌트에서 독립적으로 테스트). `store.ts`를 확장해 이벤트 이력(`events`)·재실(`present`)·최근활동시각(`lastActivityTs`)을 추가(WS 게이트웨이 데이터 재사용, 기존 필드는 불변). 계획 1·2에서 만든 `RadialGauge`·`StatCard`·`IconChip`을 재사용해 대시보드 하위 컴포넌트를 조립하고, 마지막에 `react-router-dom`으로 `App.tsx`를 라우팅 셸로 바꾼다(기존 `App.test.tsx`는 무수정으로 계속 통과).

**Tech Stack:** React 18 · Vite · TypeScript(strict) · zustand · react-router-dom(신규) · lucide-react · framer-motion(미사용, 기존 유지) · Vitest · @testing-library/react

## Global Constraints

- 레포 루트: `C:\Users\erifkim\my_projects\K-hackathon\happy-care`. 웹은 `web/`. Windows(Git Bash). 테스트 `cd web && npm run test`. 타입 `npx tsc -b`.
- **이모지 절대 금지** — 아이콘은 lucide-react만. 이번 계획에서 쓰는 아이콘은 스펙 화이트리스트 중 `ShieldCheck·TriangleAlert·HeartPulse·Activity·Bell·Home·Settings`.
- 팔레트 토큰(theme.css, 기존): `--ground:#F4F7FE --card:#FFFFFF --ink:#0F1B33 --muted:#5B6B87 --faint:#8A97B2 --line:#E4EAF5 --blue:#2E6BF6 --blue-deep:#1B4DB3 --blue-soft:#EAF1FF --good:#1FA97A --good-soft:#E3F5EE --danger:#E5484D --danger-soft:#FCE9E9 --amber:#E8A23D --shadow-sm:0 8px 22px -12px rgba(24,54,120,.28)`.
- **상태 게이지 그라데이션(이 계획에서 확정)**: safe `#1E5BE6→#5B9BFF`(RadialGauge 기본값) · warning `#E8A23D→#F3C27C` · danger `#E5484D→#F3A0A0`.
- **기존 테스트·앵커 무수정 유지**: `web/tests/store.test.ts`·`Kiosk.test.tsx`·`App.test.tsx`·`RadialGauge.test.tsx`·`IconChip.test.tsx`·`StatCard.test.tsx`(총 11 tests)는 이 계획에서 **수정하지 않는다**. `App.test.tsx`가 기대하는 `data-testid="mode"`, 낙상 `role="alert"`("낙상 감지")는 라우팅 도입 후에도 `/`(기본) 접속 시 그대로 렌더되어야 한다.
- `ts`(epoch) 값은 백엔드가 **초 단위**로 발행(`CareEvent.ts`, `PersonState.ts`) — 프론트 `lib/derive.ts`의 시간 함수는 전부 **입력을 초 단위**로 받고 내부에서 `*1000`으로 환산한다(밀리초 `now`는 예외, 아래 시그니처 참고).
- TypeScript **strict**, `verbatimModuleSyntax:true`(타입 전용 임포트는 `import type`), `noUnusedLocals/Parameters:true`.

---

## File Structure

```
web/src/
  lib/
    derive.ts                       # 신규: 순수 함수(안심상태·시간표기)
  store.ts                          # 수정: events/present/lastActivityTs 추가
  components/
    Sparkbars.tsx                   # 신규: 공유 미니 막대그래프
  theme.css                         # 수정: .bars 프리미티브 append
  views/
    dashboard/
      dashboard.css                 # 신규: 대시보드 페이지 전용 CSS
      HeroStatus.tsx                # 신규: 안심 게이지 히어로
      MetricCards.tsx               # 신규: 심박·활동 카드 2열
      HomeMap.tsx                   # 신규: 집 평면도 SVG
      ActivityFeed.tsx              # 신규: 최근 소식 타임라인
      TabBar.tsx                    # 신규: 하단 탭
      Dashboard.tsx                 # 신규: 조립
  App.tsx                           # 수정: react-router-dom 라우팅 셸
  tests/(web/tests/)
    derive.test.ts
    store.dashboard.test.ts
    HeroStatus.test.tsx
    Sparkbars.test.tsx  MetricCards.test.tsx
    HomeMap.test.tsx
    ActivityFeed.test.tsx
    TabBar.test.tsx  Dashboard.test.tsx
    App.routing.test.tsx
```

---

### Task 1: 파생 로직 (lib/derive.ts)

**Files:**
- Create: `web/src/lib/derive.ts`
- Test: `web/tests/derive.test.ts`

**Interfaces:**
- Produces:
  - `type SafetyLevel = 'safe' | 'warning' | 'danger'`
  - `interface SafetyStatus { level: SafetyLevel; title: string; subtitle: string; pill: string }`
  - `interface DeriveSafetyInput { present: boolean; lastActivityTs: number | null; hasCriticalAlert: boolean; now: number }` (`lastActivityTs`=초, `now`=밀리초)
  - `deriveSafetyStatus(input: DeriveSafetyInput): SafetyStatus`
  - `gaugeValueForLevel(level: SafetyLevel): number` — safe=0.9, warning=0.55, danger=0.25
  - `formatRelativeTime(fromTsSeconds: number, nowMs: number): string`
  - `formatClockShort(tsSeconds: number): string` — `"HH:MM"`(zero-padded, 로컬시간)

- [ ] **Step 1: 실패하는 테스트 작성**

`web/tests/derive.test.ts`:
```ts
import { expect, test } from 'vitest'
import {
  deriveSafetyStatus, gaugeValueForLevel, formatRelativeTime, formatClockShort,
} from '../src/lib/derive'

const NOW = 1_700_000_000_000 // 고정 기준 시각(ms), 실시계 비의존

test('critical alert overrides everything to danger', () => {
  const s = deriveSafetyStatus({
    present: true, lastActivityTs: NOW / 1000 - 60, hasCriticalAlert: true, now: NOW,
  })
  expect(s.level).toBe('danger')
  expect(s.title).toBe('위험')
})

test('present and recently active is safe', () => {
  const s = deriveSafetyStatus({
    present: true, lastActivityTs: NOW / 1000 - 300, hasCriticalAlert: false, now: NOW,
  })
  expect(s.level).toBe('safe')
  expect(s.title).toBe('안심')
})

test('present but activity stale beyond 4 hours is warning', () => {
  const s = deriveSafetyStatus({
    present: true, lastActivityTs: NOW / 1000 - 5 * 3600, hasCriticalAlert: false, now: NOW,
  })
  expect(s.level).toBe('warning')
  expect(s.title).toBe('주의')
})

test('absent (외출 중) stays safe even if last activity is stale', () => {
  const s = deriveSafetyStatus({
    present: false, lastActivityTs: NOW / 1000 - 5 * 3600, hasCriticalAlert: false, now: NOW,
  })
  expect(s.level).toBe('safe')
})

test('gaugeValueForLevel maps level to fill amount', () => {
  expect(gaugeValueForLevel('safe')).toBe(0.9)
  expect(gaugeValueForLevel('warning')).toBe(0.55)
  expect(gaugeValueForLevel('danger')).toBe(0.25)
})

test('formatRelativeTime buckets: just now / minutes / hours / days', () => {
  expect(formatRelativeTime(NOW / 1000 - 30, NOW)).toBe('방금 전')
  expect(formatRelativeTime(NOW / 1000 - 600, NOW)).toBe('10분 전')
  expect(formatRelativeTime(NOW / 1000 - 7200, NOW)).toBe('2시간 전')
  expect(formatRelativeTime(NOW / 1000 - 172800, NOW)).toBe('2일 전')
})

test('formatClockShort formats HH:MM with zero-padding', () => {
  const d = new Date(2026, 0, 1, 9, 5, 0)
  expect(formatClockShort(d.getTime() / 1000)).toBe('09:05')
})
```

- [ ] **Step 2: 테스트 실패 확인**

Run: `cd web && npm run test`
Expected: FAIL — cannot find `../src/lib/derive` (기존 11개는 그대로 통과)

- [ ] **Step 3: derive.ts 구현**

`web/src/lib/derive.ts`:
```ts
export type SafetyLevel = 'safe' | 'warning' | 'danger'

export interface SafetyStatus {
  level: SafetyLevel
  title: string
  subtitle: string
  pill: string
}

export interface DeriveSafetyInput {
  present: boolean
  lastActivityTs: number | null // epoch seconds
  hasCriticalAlert: boolean
  now: number // epoch ms
}

const STALE_MS = 4 * 60 * 60 * 1000 // 4시간

export function deriveSafetyStatus(input: DeriveSafetyInput): SafetyStatus {
  const { present, lastActivityTs, hasCriticalAlert, now } = input
  const activityPart = lastActivityTs != null
    ? `${formatRelativeTime(lastActivityTs, now)} 활동`
    : '활동 기록 없음'
  const presencePart = present ? '재실 중' : '외출 중'

  if (hasCriticalAlert) {
    return {
      level: 'danger',
      title: '위험',
      subtitle: '즉시 확인이 필요해요',
      pill: `${presencePart} · ${activityPart} · 확인 필요`,
    }
  }

  const isStale = lastActivityTs != null && now - lastActivityTs * 1000 > STALE_MS
  if (present && isStale) {
    return {
      level: 'warning',
      title: '주의',
      subtitle: '최근 활동이 뜸해요',
      pill: `${presencePart} · ${activityPart} · 확인 권장`,
    }
  }

  return {
    level: 'safe',
    title: '안심',
    subtitle: '오늘도 평온해요',
    pill: `${presencePart} · ${activityPart} · 이상 없음`,
  }
}

export function gaugeValueForLevel(level: SafetyLevel): number {
  if (level === 'danger') return 0.25
  if (level === 'warning') return 0.55
  return 0.9
}

export function formatRelativeTime(fromTsSeconds: number, nowMs: number): string {
  const diffMs = nowMs - fromTsSeconds * 1000
  if (diffMs < 60_000) return '방금 전'
  if (diffMs < 3_600_000) return `${Math.floor(diffMs / 60_000)}분 전`
  if (diffMs < 86_400_000) return `${Math.floor(diffMs / 3_600_000)}시간 전`
  return `${Math.floor(diffMs / 86_400_000)}일 전`
}

export function formatClockShort(tsSeconds: number): string {
  const d = new Date(tsSeconds * 1000)
  const h = d.getHours().toString().padStart(2, '0')
  const m = d.getMinutes().toString().padStart(2, '0')
  return `${h}:${m}`
}
```

- [ ] **Step 4: 테스트 통과 확인**

Run: `npm run test`
Expected: PASS (18 passed — 기존 11 + derive 7)

- [ ] **Step 5: 타입 확인 + 커밋**

Run: `npx tsc -b` (expect clean)
```bash
cd C:/Users/erifkim/my_projects/K-hackathon/happy-care
git add web/src/lib/derive.ts web/tests/derive.test.ts
git commit -m "feat(web): pure safety-status derivation + time formatting"
```

---

### Task 2: 스토어 확장 (events · present · lastActivityTs)

**Files:**
- Modify: `web/src/store.ts`
- Test: `web/tests/store.dashboard.test.ts`

**Interfaces:**
- Consumes: `CareEvent`·`SystemMode`·`WsMessage`(기존 types.ts, 불변).
- Produces: `AppState`에 `events: CareEvent[]`(최신순, 최대 20개), `present: boolean`, `lastActivityTs: number | null`(초) 추가. `handle()`이 모든 `event` 메시지를 `events` 앞에 추가(20개로 캡)하고, `state` 메시지마다 `present`·`lastActivityTs`를 갱신(기존 `mode`/`alert`/`heartBpm` 동작은 불변).

- [ ] **Step 1: 실패하는 테스트 작성**

`web/tests/store.dashboard.test.ts`:
```ts
import { beforeEach, expect, test } from 'vitest'
import { useStore } from '../src/store'

beforeEach(() => useStore.setState({
  mode: 'outdoor', alert: null, heartBpm: null,
  events: [], present: false, lastActivityTs: null,
}))

test('state message updates present and lastActivityTs', () => {
  useStore.getState().handle(JSON.stringify({
    kind: 'state',
    data: { ts: 1700000000, present: true, position: [1, 1], torso_velocity: 0, activity: 'walking', heart_bpm: 72 },
  }))
  expect(useStore.getState().present).toBe(true)
  expect(useStore.getState().lastActivityTs).toBe(1700000000)
})

test('any event type is appended to events, newest first', () => {
  useStore.getState().handle(JSON.stringify({
    kind: 'event', data: { ts: 1, type: 'mode_change', severity: 'info', mode: 'indoor', message: '도킹' },
  }))
  useStore.getState().handle(JSON.stringify({
    kind: 'event', data: { ts: 2, type: 'fall', severity: 'critical', mode: 'indoor', message: '낙상' },
  }))
  const events = useStore.getState().events
  expect(events).toHaveLength(2)
  expect(events[0].type).toBe('fall')
  expect(events[1].type).toBe('mode_change')
})

test('events list caps at 20 most recent entries', () => {
  for (let i = 0; i < 25; i++) {
    useStore.getState().handle(JSON.stringify({
      kind: 'event', data: { ts: i, type: 'mode_change', severity: 'info', mode: 'indoor', message: `e${i}` },
    }))
  }
  const events = useStore.getState().events
  expect(events).toHaveLength(20)
  expect(events[0].message).toBe('e24')
})
```

- [ ] **Step 2: 테스트 실패 확인**

Run: `npm run test`
Expected: FAIL — `events`/`present`/`lastActivityTs`가 store에 없음 (기존 18개는 통과)

- [ ] **Step 3: store.ts 확장**

`web/src/store.ts` 전체 교체:
```ts
import { create } from 'zustand'
import type { CareEvent, SystemMode, WsMessage } from './types'

const MAX_EVENTS = 20

interface AppState {
  mode: SystemMode
  alert: CareEvent | null
  heartBpm: number | null
  connected: boolean
  events: CareEvent[]
  present: boolean
  lastActivityTs: number | null
  setConnected: (v: boolean) => void
  clearAlert: () => void
  handle: (raw: string) => void
}

export const useStore = create<AppState>((set, get) => ({
  mode: 'outdoor',
  alert: null,
  heartBpm: null,
  connected: false,
  events: [],
  present: false,
  lastActivityTs: null,
  setConnected: (v) => set({ connected: v }),
  clearAlert: () => set({ alert: null }),
  handle: (raw) => {
    const msg = JSON.parse(raw) as WsMessage
    if (msg.kind === 'event') {
      if (msg.data.type === 'mode_change') set({ mode: msg.data.mode })
      if (msg.data.type === 'fall') set({ alert: msg.data })
      set({ events: [msg.data, ...get().events].slice(0, MAX_EVENTS) })
    } else if (msg.kind === 'state') {
      set({ present: msg.data.present, lastActivityTs: msg.data.ts })
      if (msg.data.heart_bpm != null) set({ heartBpm: msg.data.heart_bpm })
    }
  },
}))
```

- [ ] **Step 4: 테스트 통과 확인**

Run: `npm run test`
Expected: PASS (21 passed)

- [ ] **Step 5: 타입 확인 + 커밋**

Run: `npx tsc -b` (expect clean)
```bash
cd C:/Users/erifkim/my_projects/K-hackathon/happy-care
git add web/src/store.ts web/tests/store.dashboard.test.ts
git commit -m "feat(web): extend store with event feed, presence, last-activity"
```

---

### Task 3: HeroStatus (안심 게이지 히어로)

**Files:**
- Create: `web/src/views/dashboard/dashboard.css`
- Create: `web/src/views/dashboard/HeroStatus.tsx`
- Test: `web/tests/HeroStatus.test.tsx`

**Interfaces:**
- Consumes: `RadialGauge`(계획1, `web/src/components/RadialGauge.tsx`, props `{value,size?,stroke?,from?,to?,track?,children?}`), `gaugeValueForLevel`·`SafetyStatus`(Task 1).
- Produces: `HeroStatus(props: SafetyStatus)` — `.hero` 카드에 `RadialGauge`(size=196,stroke=16) 가운데 `.gauge-check`(danger 시 `is-danger`) + 아이콘(ShieldCheck/TriangleAlert) + `.gauge-big`(title) + `.gauge-small`(subtitle), 아래 `.hero-sub`(pill).

- [ ] **Step 1: 실패하는 테스트 작성**

`web/tests/HeroStatus.test.tsx`:
```tsx
import { render, screen } from '@testing-library/react'
import { expect, test } from 'vitest'
import { HeroStatus } from '../src/views/dashboard/HeroStatus'
import { gaugeValueForLevel } from '../src/lib/derive'

test('renders title, subtitle and pill text', () => {
  render(<HeroStatus level="safe" title="안심" subtitle="오늘도 평온해요" pill="재실 중 · 5분 전 활동 · 이상 없음" />)
  expect(screen.getByText('안심')).toBeInTheDocument()
  expect(screen.getByText('오늘도 평온해요')).toBeInTheDocument()
  expect(screen.getByText('재실 중 · 5분 전 활동 · 이상 없음')).toBeInTheDocument()
})

test('gauge fill reflects the danger level', () => {
  const { container } = render(<HeroStatus level="danger" title="위험" subtitle="즉시 확인이 필요해요" pill="확인 필요" />)
  const prog = container.querySelector('[data-role="progress"]')!
  const r = (196 - 16) / 2
  const c = 2 * Math.PI * r
  const expected = c * (1 - gaugeValueForLevel('danger'))
  expect(Number(prog.getAttribute('stroke-dashoffset'))).toBeCloseTo(expected, 2)
})
```

- [ ] **Step 2: 테스트 실패 확인**

Run: `npm run test`
Expected: FAIL — cannot find `../src/views/dashboard/HeroStatus`

- [ ] **Step 3: dashboard.css 생성 + HeroStatus 구현**

`web/src/views/dashboard/dashboard.css`:
```css
/* 보호자 대시보드 전용 레이아웃 CSS. Dashboard.tsx(Task 7)에서 import한다. */
.hero{background:var(--card);border-radius:26px;padding:24px 20px 20px;
  box-shadow:var(--shadow-sm);display:flex;flex-direction:column;align-items:center;text-align:center}
.gauge-check{width:52px;height:52px;border-radius:50%;background:var(--good-soft);
  color:var(--good);display:grid;place-items:center;margin-bottom:6px}
.gauge-check.is-danger{background:var(--danger-soft);color:var(--danger)}
.gauge-big{font-size:26px;font-weight:800;letter-spacing:-.02em;line-height:1.2}
.gauge-small{font-size:13px;color:var(--muted);font-weight:600}
.hero-sub{margin-top:16px;font-size:13.5px;font-weight:600;color:var(--blue-deep);
  background:var(--blue-soft);border-radius:999px;padding:9px 16px}
```

`web/src/views/dashboard/HeroStatus.tsx`:
```tsx
import { ShieldCheck, TriangleAlert } from 'lucide-react'
import { RadialGauge } from '../../components/RadialGauge'
import { gaugeValueForLevel } from '../../lib/derive'
import type { SafetyStatus } from '../../lib/derive'

type HeroStatusProps = SafetyStatus

const GRADIENTS: Record<SafetyStatus['level'], { from: string; to: string }> = {
  safe: { from: '#1E5BE6', to: '#5B9BFF' },
  warning: { from: '#E8A23D', to: '#F3C27C' },
  danger: { from: '#E5484D', to: '#F3A0A0' },
}

export function HeroStatus({ level, title, subtitle, pill }: HeroStatusProps) {
  const Icon = level === 'danger' ? TriangleAlert : ShieldCheck
  const { from, to } = GRADIENTS[level]
  return (
    <div className="hero">
      <RadialGauge value={gaugeValueForLevel(level)} size={196} stroke={16} from={from} to={to}>
        <span className={level === 'danger' ? 'gauge-check is-danger' : 'gauge-check'}>
          <Icon size={28} strokeWidth={2.4} aria-hidden="true" />
        </span>
        <span className="gauge-big">{title}</span>
        <span className="gauge-small">{subtitle}</span>
      </RadialGauge>
      <p className="hero-sub">{pill}</p>
    </div>
  )
}
```

- [ ] **Step 4: 테스트 통과 확인**

Run: `npm run test`
Expected: PASS (23 passed)

- [ ] **Step 5: 타입 확인 + 커밋**

Run: `npx tsc -b` (expect clean)
```bash
cd C:/Users/erifkim/my_projects/K-hackathon/happy-care
git add web/src/views/dashboard/dashboard.css web/src/views/dashboard/HeroStatus.tsx web/tests/HeroStatus.test.tsx
git commit -m "feat(web): dashboard HeroStatus safety gauge"
```

---

### Task 4: Sparkbars + MetricCards (심박·활동 카드)

**Files:**
- Create: `web/src/components/Sparkbars.tsx`
- Create: `web/src/views/dashboard/MetricCards.tsx`
- Modify: `web/src/theme.css` (`.bars` 프리미티브 append)
- Test: `web/tests/Sparkbars.test.tsx`, `web/tests/MetricCards.test.tsx`

**Interfaces:**
- Consumes: `StatCard`(계획1, `web/src/components/StatCard.tsx`, props `{icon,label,children?}`).
- Produces: `Sparkbars(props: { values: number[] })` — `.bars` 안에 값마다 `<i>` 막대(높이는 최대값 대비 비율, 최소 8%), 값이 최대값의 55% 이상이면 `on` 클래스. `MetricCards(props: { heartBpm: number | null; activity: number[] })` — `.metrics`(2열) 안에 심박 `StatCard`(값 없으면 "측정 중")와 활동량 `StatCard`(`Sparkbars`).

- [ ] **Step 1: 실패하는 테스트 작성**

`web/tests/Sparkbars.test.tsx`:
```tsx
import { render } from '@testing-library/react'
import { expect, test } from 'vitest'
import { Sparkbars } from '../src/components/Sparkbars'

test('renders one bar per value', () => {
  const { container } = render(<Sparkbars values={[10, 40, 25, 60]} />)
  expect(container.querySelectorAll('.bars > i')).toHaveLength(4)
})

test('marks bars at or above 55% of the max as active', () => {
  const { container } = render(<Sparkbars values={[10, 100]} />)
  const bars = container.querySelectorAll('.bars > i')
  expect(bars[0].className).not.toContain('on')
  expect(bars[1].className).toContain('on')
})
```

`web/tests/MetricCards.test.tsx`:
```tsx
import { render, screen } from '@testing-library/react'
import { expect, test } from 'vitest'
import { MetricCards } from '../src/views/dashboard/MetricCards'

test('shows heart rate value and label', () => {
  render(<MetricCards heartBpm={72} activity={[10, 20, 30]} />)
  expect(screen.getByText('심박')).toBeInTheDocument()
  expect(screen.getByText('72')).toBeInTheDocument()
})

test('shows a placeholder while heart rate is not yet known', () => {
  render(<MetricCards heartBpm={null} activity={[10, 20, 30]} />)
  expect(screen.getByText('측정 중')).toBeInTheDocument()
})
```

- [ ] **Step 2: 테스트 실패 확인**

Run: `npm run test`
Expected: FAIL — cannot find `../src/components/Sparkbars` / `../src/views/dashboard/MetricCards`

- [ ] **Step 3: theme.css append + 컴포넌트 구현**

`web/src/theme.css` 끝에 추가:
```css
.bars{display:flex;align-items:flex-end;gap:4px;height:30px}
.bars i{flex:1;background:var(--blue-soft);border-radius:3px;font-style:normal}
.bars i.on{background:var(--blue)}
```

`web/src/components/Sparkbars.tsx`:
```tsx
interface SparkbarsProps {
  values: number[]
}

export function Sparkbars({ values }: SparkbarsProps) {
  const max = Math.max(1, ...values)
  return (
    <div className="bars">
      {values.map((v, i) => (
        <i
          key={i}
          className={v >= max * 0.55 ? 'on' : ''}
          style={{ height: `${Math.max(8, (v / max) * 100)}%` }}
        />
      ))}
    </div>
  )
}
```

`web/src/views/dashboard/MetricCards.tsx`:
```tsx
import { Activity, HeartPulse } from 'lucide-react'
import { StatCard } from '../../components/StatCard'
import { Sparkbars } from '../../components/Sparkbars'

interface MetricCardsProps {
  heartBpm: number | null
  activity: number[]
}

export function MetricCards({ heartBpm, activity }: MetricCardsProps) {
  return (
    <div className="metrics">
      <StatCard icon={HeartPulse} label="심박">
        {heartBpm != null ? (
          <>
            {heartBpm}
            <span className="u">회/분</span>
          </>
        ) : (
          '측정 중'
        )}
      </StatCard>
      <StatCard icon={Activity} label="활동량">
        <Sparkbars values={activity} />
      </StatCard>
    </div>
  )
}
```

`web/src/views/dashboard/dashboard.css` 끝에 추가:
```css
.metrics{display:grid;grid-template-columns:1fr 1fr;gap:12px}
```

- [ ] **Step 4: 테스트 통과 확인**

Run: `npm run test`
Expected: PASS (27 passed)

- [ ] **Step 5: 타입 확인 + 커밋**

Run: `npx tsc -b` (expect clean)
```bash
cd C:/Users/erifkim/my_projects/K-hackathon/happy-care
git add web/src/theme.css web/src/components/Sparkbars.tsx web/src/views/dashboard/MetricCards.tsx web/src/views/dashboard/dashboard.css web/tests/Sparkbars.test.tsx web/tests/MetricCards.test.tsx
git commit -m "feat(web): Sparkbars primitive + dashboard MetricCards"
```

---

### Task 5: HomeMap (집 평면도)

**Files:**
- Create: `web/src/views/dashboard/HomeMap.tsx`
- Modify: `web/src/views/dashboard/dashboard.css` (map 클래스 append)
- Test: `web/tests/HomeMap.test.tsx`

**Interfaces:**
- Produces: `HomeMap(props: { mode: 'indoor' | 'outdoor'; room?: string })`(기본 `room='거실'`) — `.map-card`에 `.map-badge`(`data-testid="map-badge"`, 텍스트 "실내 모드"/"실외 모드") + SVG 평면도(4개 방, indoor 시 현재 방 `.room.is-active` + `data-testid="here-dot"` 위치점).

- [ ] **Step 1: 실패하는 테스트 작성**

`web/tests/HomeMap.test.tsx`:
```tsx
import { render, screen } from '@testing-library/react'
import { expect, test } from 'vitest'
import { HomeMap } from '../src/views/dashboard/HomeMap'

test('shows indoor badge and highlights the current room', () => {
  const { container } = render(<HomeMap mode="indoor" room="거실" />)
  expect(screen.getByTestId('map-badge')).toHaveTextContent('실내 모드')
  expect(container.querySelector('.room.is-active')).toBeTruthy()
  expect(screen.getByTestId('here-dot')).toBeInTheDocument()
})

test('shows outdoor badge with no position dot', () => {
  render(<HomeMap mode="outdoor" />)
  expect(screen.getByTestId('map-badge')).toHaveTextContent('실외 모드')
  expect(screen.queryByTestId('here-dot')).not.toBeInTheDocument()
})
```

- [ ] **Step 2: 테스트 실패 확인**

Run: `npm run test`
Expected: FAIL — cannot find `../src/views/dashboard/HomeMap`

- [ ] **Step 3: dashboard.css append + HomeMap 구현**

`web/src/views/dashboard/dashboard.css` 끝에 추가:
```css
.map-card{background:var(--card);border-radius:20px;padding:15px 16px;box-shadow:var(--shadow-sm)}
.map-top{display:flex;align-items:center;justify-content:space-between;
  font-size:13px;font-weight:700;color:var(--muted)}
.map-badge{font-size:11.5px;font-weight:700;color:var(--blue);
  background:var(--blue-soft);border-radius:999px;padding:3px 10px}
.plan{margin-top:12px;width:100%;height:104px;display:block}
.plan .room{fill:#fff;stroke:var(--line);stroke-width:1.5}
.plan .room.is-active{fill:var(--blue-soft);stroke:var(--blue)}
```

`web/src/views/dashboard/HomeMap.tsx`:
```tsx
interface HomeMapProps {
  mode: 'indoor' | 'outdoor'
  room?: string
}

export function HomeMap({ mode, room = '거실' }: HomeMapProps) {
  return (
    <div className="map-card">
      <div className="map-top">
        <span>집 · 실시간 위치</span>
        <span className="map-badge" data-testid="map-badge">
          {mode === 'indoor' ? '실내 모드' : '실외 모드'}
        </span>
      </div>
      <svg className="plan" viewBox="0 0 240 104" role="img" aria-label={`현재 위치: ${room}`}>
        <rect className={mode === 'indoor' ? 'room is-active' : 'room'} x="2" y="2" width="120" height="58" rx="10" />
        <rect className="room" x="128" y="2" width="110" height="58" rx="10" />
        <rect className="room" x="2" y="66" width="78" height="36" rx="10" />
        <rect className="room" x="86" y="66" width="152" height="36" rx="10" />
        <text x="14" y="32" fontSize="12" fontWeight="700" fill="var(--blue-deep)">{room}</text>
        <text x="140" y="34" fontSize="12" fontWeight="700" fill="var(--faint)">침실</text>
        <text x="14" y="88" fontSize="11" fontWeight="700" fill="var(--faint)">주방</text>
        <text x="98" y="88" fontSize="11" fontWeight="700" fill="var(--faint)">화장실</text>
        {mode === 'indoor' && <circle data-testid="here-dot" cx="70" cy="40" r="8" fill="var(--blue)" />}
      </svg>
    </div>
  )
}
```

- [ ] **Step 4: 테스트 통과 확인**

Run: `npm run test`
Expected: PASS (29 passed)

- [ ] **Step 5: 타입 확인 + 커밋**

Run: `npx tsc -b` (expect clean)
```bash
cd C:/Users/erifkim/my_projects/K-hackathon/happy-care
git add web/src/views/dashboard/HomeMap.tsx web/src/views/dashboard/dashboard.css web/tests/HomeMap.test.tsx
git commit -m "feat(web): dashboard HomeMap floor plan"
```

---

### Task 6: ActivityFeed (최근 소식)

**Files:**
- Create: `web/src/views/dashboard/ActivityFeed.tsx`
- Modify: `web/src/views/dashboard/dashboard.css` (feed 클래스 append)
- Test: `web/tests/ActivityFeed.test.tsx`

**Interfaces:**
- Consumes: `CareEvent`(types.ts), `formatClockShort`(Task 1).
- Produces: `ActivityFeed(props: { events: CareEvent[] })` — `.feed`에 제목 + 각 이벤트 행(`.t` 시각, `.d`+`severity`별 톤 dot, `.txt` 메시지). 빈 배열이면 `.feed-empty` 안내 문구.

- [ ] **Step 1: 실패하는 테스트 작성**

`web/tests/ActivityFeed.test.tsx`:
```tsx
import { render, screen } from '@testing-library/react'
import { expect, test } from 'vitest'
import { ActivityFeed } from '../src/views/dashboard/ActivityFeed'
import type { CareEvent } from '../src/types'

const events: CareEvent[] = [
  { ts: 1700000000, type: 'fall', severity: 'critical', mode: 'indoor', message: '낙상 감지 — 보호자·119 알림' },
  { ts: 1699999000, type: 'mode_change', severity: 'info', mode: 'indoor', message: '도킹 · 실내 모드 · 카메라 OFF' },
]

test('renders each event message with a tone dot', () => {
  const { container } = render(<ActivityFeed events={events} />)
  expect(screen.getByText('낙상 감지 — 보호자·119 알림')).toBeInTheDocument()
  expect(screen.getByText('도킹 · 실내 모드 · 카메라 OFF')).toBeInTheDocument()
  expect(container.querySelector('.d.is-danger')).toBeTruthy()
})

test('shows an empty state when there are no events', () => {
  render(<ActivityFeed events={[]} />)
  expect(screen.getByText('아직 기록된 소식이 없어요')).toBeInTheDocument()
})
```

- [ ] **Step 2: 테스트 실패 확인**

Run: `npm run test`
Expected: FAIL — cannot find `../src/views/dashboard/ActivityFeed`

- [ ] **Step 3: dashboard.css append + ActivityFeed 구현**

`web/src/views/dashboard/dashboard.css` 끝에 추가:
```css
.feed{padding-bottom:4px}
.feed h3{font-size:14px;font-weight:800;margin:0 0 8px}
.feed .row{display:flex;align-items:center;gap:11px;padding:9px 2px}
.feed .t{font-size:12px;color:var(--faint);font-weight:700;width:44px;font-variant-numeric:tabular-nums}
.feed .d{width:9px;height:9px;border-radius:50%;background:var(--blue);flex:none}
.feed .d.is-warning{background:var(--amber)}
.feed .d.is-danger{background:var(--danger)}
.feed .txt{font-size:13.5px;font-weight:600}
.feed-empty{font-size:13.5px;color:var(--muted)}
```

`web/src/views/dashboard/ActivityFeed.tsx`:
```tsx
import type { CareEvent } from '../../types'
import { formatClockShort } from '../../lib/derive'

interface ActivityFeedProps {
  events: CareEvent[]
}

function dotTone(severity: CareEvent['severity']): string {
  if (severity === 'critical') return 'd is-danger'
  if (severity === 'warning') return 'd is-warning'
  return 'd'
}

export function ActivityFeed({ events }: ActivityFeedProps) {
  return (
    <div className="feed">
      <h3>최근 소식</h3>
      {events.length === 0 ? (
        <p className="feed-empty">아직 기록된 소식이 없어요</p>
      ) : (
        events.map((e, i) => (
          <div className="row" key={`${e.ts}-${i}`}>
            <span className="t">{formatClockShort(e.ts)}</span>
            <span className={dotTone(e.severity)} />
            <span className="txt">{e.message}</span>
          </div>
        ))
      )}
    </div>
  )
}
```

- [ ] **Step 4: 테스트 통과 확인**

Run: `npm run test`
Expected: PASS (31 passed)

- [ ] **Step 5: 타입 확인 + 커밋**

Run: `npx tsc -b` (expect clean)
```bash
cd C:/Users/erifkim/my_projects/K-hackathon/happy-care
git add web/src/views/dashboard/ActivityFeed.tsx web/src/views/dashboard/dashboard.css web/tests/ActivityFeed.test.tsx
git commit -m "feat(web): dashboard ActivityFeed timeline"
```

---

### Task 7: TabBar + Dashboard 조립

**Files:**
- Create: `web/src/views/dashboard/TabBar.tsx`
- Create: `web/src/views/dashboard/Dashboard.tsx`
- Modify: `web/src/views/dashboard/dashboard.css` (tabbar·appbar 클래스 append)
- Test: `web/tests/TabBar.test.tsx`, `web/tests/Dashboard.test.tsx`

**Interfaces:**
- Consumes: `useStore`(Task 2 확장분), `deriveSafetyStatus`(Task 1), `HeroStatus`(Task 3), `MetricCards`(Task 4), `HomeMap`(Task 5), `ActivityFeed`(Task 6).
- Produces: `TabBar(props: { active: 'home' | 'activity' | 'alerts' | 'settings' })`. `Dashboard()` — 기본 export 없는 named export 컴포넌트, store를 읽어 위 컴포넌트를 조립.

- [ ] **Step 1: 실패하는 테스트 작성**

`web/tests/TabBar.test.tsx`:
```tsx
import { render, screen } from '@testing-library/react'
import { expect, test } from 'vitest'
import { TabBar } from '../src/views/dashboard/TabBar'

test('renders four tabs with the active one marked', () => {
  render(<TabBar active="home" />)
  expect(screen.getByText('홈').closest('.tab')?.className).toContain('on')
  expect(screen.getByText('활동')).toBeInTheDocument()
  expect(screen.getByText('알림')).toBeInTheDocument()
  expect(screen.getByText('설정')).toBeInTheDocument()
})
```

`web/tests/Dashboard.test.tsx`:
```tsx
import { render, screen } from '@testing-library/react'
import { beforeEach, expect, test } from 'vitest'
import { Dashboard } from '../src/views/dashboard/Dashboard'
import { useStore } from '../src/store'

beforeEach(() => {
  const nowSec = Math.floor(Date.now() / 1000)
  useStore.setState({
    mode: 'indoor', alert: null, heartBpm: 72,
    events: [{ ts: nowSec, type: 'mode_change', severity: 'info', mode: 'indoor', message: '도킹' }],
    present: true, lastActivityTs: nowSec,
  })
})

test('renders hero, metrics, map, feed and tab bar together', () => {
  render(<Dashboard />)
  expect(screen.getByText('안녕하세요')).toBeInTheDocument()
  expect(screen.getByText('안심')).toBeInTheDocument()
  expect(screen.getByText('심박')).toBeInTheDocument()
  expect(screen.getByTestId('map-badge')).toHaveTextContent('실내 모드')
  expect(screen.getByText('도킹')).toBeInTheDocument()
  expect(screen.getByText('홈')).toBeInTheDocument()
})
```

- [ ] **Step 2: 테스트 실패 확인**

Run: `npm run test`
Expected: FAIL — cannot find `../src/views/dashboard/TabBar` / `Dashboard`

- [ ] **Step 3: dashboard.css append + TabBar·Dashboard 구현**

`web/src/views/dashboard/dashboard.css` 끝에 추가:
```css
.dash{max-width:430px;margin:0 auto;min-height:100%;
  display:flex;flex-direction:column;gap:18px;padding:22px 18px 96px}
.dash-appbar{display:flex;align-items:center;justify-content:space-between}
.dash-appbar .hi{font-size:13px;color:var(--muted);font-weight:600}
.dash-appbar .who{font-size:20px;font-weight:800;letter-spacing:-.01em}
.iconbtn{width:42px;height:42px;border-radius:14px;background:var(--card);
  border:1px solid var(--line);display:grid;place-items:center;color:var(--ink)}

.tabbar{display:flex;justify-content:space-around;padding:12px 6px 4px;
  background:var(--card);border-top:1px solid var(--line);border-radius:0 0 22px 22px}
.tab{display:flex;flex-direction:column;align-items:center;gap:4px;font-size:11px;font-weight:700;color:var(--faint)}
.tab.on{color:var(--blue)}
```

`web/src/views/dashboard/TabBar.tsx`:
```tsx
import { Activity, Bell, Home, Settings } from 'lucide-react'
import type { LucideIcon } from 'lucide-react'

type TabKey = 'home' | 'activity' | 'alerts' | 'settings'

interface TabBarProps {
  active: TabKey
}

const TABS: { key: TabKey; label: string; icon: LucideIcon }[] = [
  { key: 'home', label: '홈', icon: Home },
  { key: 'activity', label: '활동', icon: Activity },
  { key: 'alerts', label: '알림', icon: Bell },
  { key: 'settings', label: '설정', icon: Settings },
]

export function TabBar({ active }: TabBarProps) {
  return (
    <nav className="tabbar" aria-label="보호자 앱 내비게이션">
      {TABS.map(({ key, label, icon: Icon }) => (
        <span key={key} className={key === active ? 'tab on' : 'tab'}>
          <Icon size={20} strokeWidth={2.2} aria-hidden="true" />
          {label}
        </span>
      ))}
    </nav>
  )
}
```

`web/src/views/dashboard/Dashboard.tsx`:
```tsx
import { useEffect, useState } from 'react'
import { Bell } from 'lucide-react'
import './dashboard.css'
import { useStore } from '../../store'
import { deriveSafetyStatus } from '../../lib/derive'
import { HeroStatus } from './HeroStatus'
import { MetricCards } from './MetricCards'
import { HomeMap } from './HomeMap'
import { ActivityFeed } from './ActivityFeed'
import { TabBar } from './TabBar'

function useNow(intervalMs = 30_000): number {
  const [now, setNow] = useState(() => Date.now())
  useEffect(() => {
    const id = setInterval(() => setNow(Date.now()), intervalMs)
    return () => clearInterval(id)
  }, [intervalMs])
  return now
}

export function Dashboard() {
  const mode = useStore((s) => s.mode)
  const alert = useStore((s) => s.alert)
  const heartBpm = useStore((s) => s.heartBpm)
  const present = useStore((s) => s.present)
  const lastActivityTs = useStore((s) => s.lastActivityTs)
  const events = useStore((s) => s.events)
  const now = useNow()

  const status = deriveSafetyStatus({
    present,
    lastActivityTs,
    hasCriticalAlert: alert?.severity === 'critical',
    now,
  })

  return (
    <div className="dash">
      <div className="dash-appbar">
        <div>
          <div className="hi">안녕하세요</div>
          <div className="who">어머니 상태예요</div>
        </div>
        <span className="iconbtn" aria-label="알림">
          <Bell size={20} strokeWidth={2.2} aria-hidden="true" />
        </span>
      </div>

      <HeroStatus {...status} />
      {/* 활동량은 8주 스켈레톤 단계의 시뮬레이션 값 — 실제 활동 스트림은 후속 계획에서 연동 */}
      <MetricCards heartBpm={heartBpm} activity={[40, 65, 50, 75, 60, 45]} />
      <HomeMap mode={mode} />
      <ActivityFeed events={events} />
      <TabBar active="home" />
    </div>
  )
}
```

- [ ] **Step 4: 테스트 통과 확인**

Run: `npm run test`
Expected: PASS (33 passed)

- [ ] **Step 5: 타입 확인 + 커밋**

Run: `npx tsc -b` (expect clean)
```bash
cd C:/Users/erifkim/my_projects/K-hackathon/happy-care
git add web/src/views/dashboard/TabBar.tsx web/src/views/dashboard/Dashboard.tsx web/src/views/dashboard/dashboard.css web/tests/TabBar.test.tsx web/tests/Dashboard.test.tsx
git commit -m "feat(web): dashboard TabBar + Dashboard assembly"
```

---

### Task 8: 라우팅 (react-router-dom)

**Files:**
- Modify: `web/src/App.tsx`
- Test: `web/tests/App.routing.test.tsx`
- (No modification): `web/tests/App.test.tsx` must remain untouched and pass.

**Interfaces:**
- Consumes: `Kiosk`(계획2), `Dashboard`(Task 7), `useWebSocket`(기존).
- Produces: `App.tsx`가 `BrowserRouter`로 `/kiosk`(Kiosk) · `/dashboard`(Dashboard) 라우팅, 그 외 경로는 `/kiosk`로 리다이렉트.

- [ ] **Step 1: react-router-dom 설치**

```bash
cd C:/Users/erifkim/my_projects/K-hackathon/happy-care/web
npm install react-router-dom
```

- [ ] **Step 2: 실패하는 라우팅 테스트 작성**

`web/tests/App.routing.test.tsx`:
```tsx
import { render, screen } from '@testing-library/react'
import { afterEach, beforeEach, expect, test, vi } from 'vitest'
import { App } from '../src/App'
import { useStore } from '../src/store'

class MockWS {
  onmessage: ((e: { data: string }) => void) | null = null
  constructor(public url: string) {}
  close() {}
}

beforeEach(() => {
  useStore.setState({ mode: 'outdoor', alert: null, heartBpm: null })
  vi.stubGlobal('WebSocket', MockWS as unknown as typeof WebSocket)
  window.history.pushState({}, '', '/')
})

afterEach(() => {
  window.history.pushState({}, '', '/')
})

test('default path redirects to the kiosk screen', () => {
  render(<App />)
  expect(screen.getByTestId('mode')).toBeInTheDocument()
})

test('"/dashboard" path renders the caregiver dashboard', () => {
  window.history.pushState({}, '', '/dashboard')
  render(<App />)
  expect(screen.getByText('안녕하세요')).toBeInTheDocument()
})
```

- [ ] **Step 3: 테스트 실패 확인**

Run: `npm run test`
Expected: FAIL — `App.tsx`가 아직 `/dashboard`를 렌더하지 않음(현재는 항상 Kiosk만 렌더). 기존 `App.test.tsx`(1개)는 계속 통과.

- [ ] **Step 4: App.tsx 라우팅으로 교체**

`web/src/App.tsx` 전체 교체:
```tsx
import { BrowserRouter, Navigate, Route, Routes } from 'react-router-dom'
import { Kiosk } from './views/Kiosk'
import { Dashboard } from './views/dashboard/Dashboard'
import { useWebSocket } from './ws'

const WS_URL = import.meta.env.VITE_WS_URL ?? 'ws://localhost:8000/ws'

export function App() {
  useWebSocket(WS_URL)
  return (
    <BrowserRouter>
      <Routes>
        <Route path="/kiosk" element={<Kiosk />} />
        <Route path="/dashboard" element={<Dashboard />} />
        <Route path="*" element={<Navigate to="/kiosk" replace />} />
      </Routes>
    </BrowserRouter>
  )
}

export default App
```

- [ ] **Step 5: 전체 테스트 통과 확인 (기존 App.test.tsx 포함)**

Run: `npm run test`
Expected: PASS (35 passed — 기존 `App.test.tsx`의 낙상 경보 어서션도 그대로 통과, `/`가 `/kiosk`로 리다이렉트되므로)

- [ ] **Step 6: 타입 확인 + 수동 스모크(선택)**

Run: `npx tsc -b` (expect clean)

터미널 A: `cd backend && . .venv/Scripts/activate && uvicorn happy_care.app:app`
터미널 B: `cd web && npm run dev` → `http://localhost:5173/dashboard` 접속 시 안심 게이지·심박/활동 카드·집 지도·최근 소식·하단 탭이 보이면 성공. `http://localhost:5173/`는 여전히 기존 키오스크.

- [ ] **Step 7: 커밋**

```bash
cd C:/Users/erifkim/my_projects/K-hackathon/happy-care
git add web/package.json web/package-lock.json web/src/App.tsx web/tests/App.routing.test.tsx
git commit -m "feat(web): route /kiosk and /dashboard with react-router-dom"
```

---

## Self-Review

**1. Spec coverage:**
- §1 토큰 재사용(신규 없음, 기존 theme.css 값만 참조) — Task 3~7 전부 기존 `var(--...)` 사용 ✅
- §2 공통 컴포넌트: `RadialGauge`(Task3 재사용) · `StatCard`(Task4 재사용) · `Sparkbars`(Task4 신규) · `IconChip`(StatCard 내부 재사용) ✅
- §3.1 홈: 앱바(Task7) · 히어로 게이지+안심도 산출(Task1·3) · 미니 카드 2열(Task4) · 집 지도 카드(Task5) · 최근 소식(Task6) · 하단 탭(Task7) ✅
- §3.3 데이터 소스: 기존 WS 게이트웨이 재사용(Task2 store 확장), 활동량은 시뮬 값 + 코드 주석으로 표기(Task7) ✅
- §5 라우팅: `react-router-dom`, `/` → `/kiosk` 기본, `/dashboard` 보호자(Task8) ✅
- §5 `lib/derive.ts` 파생 로직 분리(Task1) ✅
- §7 검증: 기존 11 테스트 무수정 유지 + 신규 단위/렌더 테스트 24개(derive7·store3·HeroStatus2·Sparkbars2·MetricCards2·HomeMap2·ActivityFeed2·TabBar1·Dashboard1·routing2) ✅
- §3.2(활동/알림/설정 탭 콘텐츠), Recharts 트렌드 차트는 **범위 밖**(스펙에 "후속"으로 명시) — 이 계획에서 다루지 않음, TabBar는 표시만 하고 다른 탭 콘텐츠는 없음(의도된 축소) ✅

**2. Placeholder scan:** TODO/TBD/"적절히" 없음. 모든 스텝에 완전한 코드·정확한 명령·기대 출력 포함. `Dashboard.tsx`의 시뮬 활동값은 TODO가 아니라 스펙이 명시한 "시뮬 데이터 + 라벨 표기" 요구를 코드 주석으로 이행한 것 ✅

**3. Type consistency:**
- `SafetyStatus{level,title,subtitle,pill}` — Task1 정의 → Task3 `HeroStatusProps = SafetyStatus`로 그대로 소비, Task7 `deriveSafetyStatus(...)` 반환을 `<HeroStatus {...status}/>`로 스프레드 — 필드명 일치 ✅
- `DeriveSafetyInput{present,lastActivityTs,hasCriticalAlert,now}` — Task7에서 store의 `present`·`lastActivityTs`(Task2 정의)와 `alert?.severity==='critical'`로 정확히 구성 ✅
- `RadialGauge`·`StatCard`·`IconChip` 시그니처(계획1·2) — 이 계획에서 **수정하지 않고 그대로 소비**, 새 prop 추가 없음 ✅
- `CareEvent{ts,type,severity,mode,message}`(types.ts, 불변) — Task2 store·Task6 ActivityFeed·Task7 Dashboard 전부 동일 필드명 사용 ✅
- store `events/present/lastActivityTs` — Task2 정의 → Task7 `useStore((s)=>s.events)` 등으로 정확히 소비 ✅

---

## 다음 계획 (범위 밖, 후속)
- 활동/알림/설정 탭 콘텐츠 + Recharts 트렌드 차트(돌봄진단)
- Gazebo 시뮬 → 실제 PersonState 스트림 연동(더미 소스 교체)
- 낙상 micro-Doppler CNN, 생체·고독사 상태머신
